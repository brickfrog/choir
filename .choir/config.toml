prenotify_checks = ["moon check --target native", "CHOIR_TEST_NO_EXEC=1 moon test --target native", "CHOIR_TEST_REAL_EXEC=1 moon test --target native src/exec", "moon run src/bin/choir_lint --target native"]

[project]
verify_cmd = "moon check --target native && moon test --target native"

# Model selection intentionally unset: this ChatGPT-account codex only exposes
# gpt-5.5 (spark/codex-mini variants 400), so a per-role model split adds nothing.
# The quota lever here is reasoning EFFORT (global config is xhigh on everything);
# per-role effort lands via choir-mn0l (e.g. effort.codex.dev = "low").
