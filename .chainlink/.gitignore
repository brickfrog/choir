# Machine-local overrides (not committed)
rules.local/
hook-config.local.json
.cache/
agent.json
.locks-cache/

# SQLite database — binary, changes on every operation, not useful in git
issues.db
issues.db-shm
issues.db-wal
